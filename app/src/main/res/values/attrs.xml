<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="RecyclerViewEx">
        <attr name="view_title" format="string" />
        <attr name="view_introduce" format="string" />
        <attr name="more_btn_text" format="string" />
        <attr name="introduce_src" format="reference" />
        <attr name="introduce_area_visibility" format="enum">
            <enum name="gone" value="0" />
            <enum name="visible" value="1" />
            <enum name="invisible" value="2" />
        </attr>
        <attr name="more_btn_visibility" format="enum">
            <enum name="gone" value="0" />
            <enum name="visible" value="1" />
            <enum name="invisible" value="2" />
        </attr>
    </declare-styleable>
</resources>
